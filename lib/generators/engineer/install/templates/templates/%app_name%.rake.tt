namespace "<%= app_name %>" do

  desc "Update <%= app_name %>'s migrations and static assets"
  task :update => ["update:migrations", "update:assets"]

  namespace :update do
    desc "Copy pending <%= app_name %> migrations to db/migrations"
    task :migrations do
    end

    desc "Copy <%= app_name %>'s static assets to public/"
    task :assets do
    end
  end
end